[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
boto3 = "1.36.9"
aws-sam-cli = "1.132.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
